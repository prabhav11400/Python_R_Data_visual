#DATA TYPES IN R PROGRAMMING
#Logical, Numeric, Interger, Complex, Character, Raw
#these are basic data types.
# num<-10 by default NUMERIC data type
#numeric= 13, -24, 34.567, etc
# Integer= 36L, (only 36 is numeric considered)
# Complex= 5+2i (all complex numbers complex data types me store honge)
# logical= true, false
# character single quoted, or double quoted, both works
# (in c, single quote me only one charater, double quote me string store krte the)
# but in R,PYTHON both single and double quote se character/ string store kr skte hain, since
# charater datatype charater and string dono data types ko store krta hai.
# character= 'a' , 'aman', "a", "aman" etc are stored as strings .
# raw= unusefull info ke liye raw.
 num<-10.256;
 class(num)
typeof(num); 
  int2<-15
class(int2)
typeof(int2)
  int2<-15L
class(int2)
typeof(int2)
comp<--10+25i
class(comp)
typeof(comp)
logi<-TRUE
class(logi)
typeof(logi)
char<-"abcdef"
class(char)
typeof(char)
char2<-       
